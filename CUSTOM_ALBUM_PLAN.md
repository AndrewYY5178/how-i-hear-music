# 自选专辑 — 待用户审核

## 位置与边界

放在 Archive → Albums 内的「自选专辑」子视图，不增加第六个主模块。Home / Archive / Rate / Taste / Import 保持不变。先做私人编选，不上传音频、不提供下载或公开发布，不改写原专辑、原作品和原评分。

## UI

桌面：左侧档案曲目选择器，右侧专辑编排；方形封面、对齐元数据、1px 分隔线、直角控件，无浮动卡片或阴影。沿用纸张/黑白两主题、Libre Baskerville 与 DM Mono。搜索不设占位提示词，使用可访问名称。

手机：选曲 → 编排 → 预览；每一步保持选择，底部继续保留五模块导航。曲序支持拖动，也有 44px 上移/下移按钮和键盘操作。草图仅表示结构：最终控件不同时重复提供勾选与加号，已选状态与右侧列表必须一致。

预览沿用专辑详情的封面、曲序与色场语言；显示「私人编选」区别正式发行。封面可用所选作品的四格拼贴，或用户上传的图片。空封面、单曲、长标题均有确定布局。

## 实现顺序（批准后）

1. 新增独立 compilation 数据：id、ownerId、title、description、cover、orderedTrackRefs、createdAt、updatedAt、schemaVersion。每个引用包含作品和具体版本 ID，保持现场/录音室等版本区别。
2. 从当前用户档案选择作品；整张专辑添加时展开为确认过的曲序；不存在的作品先经 Import 进入档案。禁止悄悄补齐评分。
3. 提供增删、排序、命名、封面、编选说明、保存草稿与预览；空列表不可完成保存，删除需确认。
4. 本地按账号隔离存储，随后接入现有账号同步及备份格式。冲突保留副本，旧备份可迁移；退出后不得出现私人编选。游客只操作演示副本。
5. 原作品被删时显示失效引用并允许替换/移除，不能删除整个编选或误绑定同名版本。作品评分保持独立；未评分不算零，不改变 Taste 统计。
6. 验收 390 / 1024 / 1440px、两主题、键盘排序、刷新恢复、跨账号隔离、备份恢复与丢失引用，再考虑上线。

## 审核点

请审核入口位置、左右编选布局与手机三步流程。当前仅新增计划和生成草图，未新增网站功能。
