# 自选专辑 — 待用户审核

## 位置与边界

放在 Archive → Albums 内的「自选专辑」子视图，不增加第六个主模块。Home / Archive / Rate / Taste / Import 保持不变。先做私人编选，不上传音频、不提供下载或公开发布，不改写原专辑、原作品和原评分。

## UI

桌面：左侧档案曲目选择器，右侧专辑编排；方形封面、对齐元数据、1px 分隔线、直角控件，无浮动卡片或阴影。沿用纸张/黑白两主题、Libre Baskerville 与 DM Mono。搜索不设占位提示词，使用可访问名称。

手机：选曲 → 编排 → 预览；每一步保持选择，底部继续保留五模块导航。曲序支持拖动，也有 44px 上移/下移按钮和键盘操作。草图仅表示结构：最终控件不同时重复提供勾选与加号，已选状态与右侧列表必须一致。

预览沿用专辑详情的封面、曲序与色场语言；显示「私人编选」区别正式发行。封面可用所选作品的四格拼贴，或用户上传的图片。空封面、单曲、长标题均有确定布局。

## 实现顺序（批准后）

1. 新增独立 compilation 数据：id、ownerId、title、description、cover、orderedTrackRefs、createdAt、updatedAt、schemaVersion。每个引用包含作品和具体版本 ID，保持现场/录音室等版本区别。
2. 从当前用户档案选择作品；整张专辑添加时展开为确认过的曲序；不存在的作品先经 Import 进入档案。禁止悄悄补齐评分。
3. 提供增删、排序、命名、封面、编选说明、保存草稿与预览；空列表不可完成保存，删除需确认。
4. 本地按账号隔离存储，随后接入现有账号同步及备份格式。冲突保留副本，旧备份可迁移；退出后不得出现私人编选。游客只操作演示副本。
5. 原作品被删时显示失效引用并允许替换/移除，不能删除整个编选或误绑定同名版本。作品评分保持独立；未评分不算零，不改变 Taste 统计。
6. 验收 390 / 1024 / 1440px、两主题、键盘排序、刷新恢复、跨账号隔离、备份恢复与丢失引用，再考虑上线。

## 封面自主设计（待审核）

在「我的编选」封面上提供「设计封面」入口；桌面打开大编辑面板，手机进入全屏编辑步骤，仍属于 Archive，不增加主模块。推荐轻量封面工作室，不做完整绘图软件。

### 三种起点，共用一个编辑器

- 拼贴模板：提供单图、二分、四格和纯文字版式；选择编选中的作品封面或上传自己的图片，允许替换、调序、裁切和调整留白。更换模板前保留草稿，不能无提示丢失设计。
- 自由排版：固定 1:1 画布；首版最多四个图片图层、两个文字图层。图片可移动、缩放、旋转和调整前后层级；提供对齐吸附、安全边距及居中操作。文字可编辑标题/副标题、字号、颜色、对齐，并使用少量内置中英文字体。背景支持纯色和可开关的纸张纹理。
- 整图上传：用户可直接使用外部设计好的封面，只需方形裁切，无须添加网站标记或套用模板。

编辑器界面沿用 HIM 视觉规范，但不强制用户封面使用网站配色；提供封面提取色和自选颜色。预览实时显示方形封面及缩略图效果。撤销/重做、重置确认、自动保存草稿、取消后保留原成品；更换曲目不自动重做用户封面。

### 数据与渲染

- 保存版本化 coverDesign（画布、模板、图层、资源引用、裁切位置、文字、颜色）与独立成品图，确保以后能继续编辑，不只保存扁平图片。
- 使用统一的画布坐标与渲染逻辑，先以 1200×1200 导出 WebP/PNG；等待图片与字体加载完成，页面展示压缩缩略图，原设计仍可恢复。具体渲染库在实施时评估。
- 上传先验证真实图片类型、文件大小与解码尺寸；首版只接受 JPEG/PNG/WebP。处理手机图片方向，导出不保留定位等原始元数据。
- 远程作品封面仅经现有允许来源的图片代理读取；来源不可读时保留原设计，提示替换图片，不能无声输出缺图成品。
- 图片二进制保存在 IndexedDB，设计记录按账号隔离；云同步须确认现有服务的图片容量与配额后实现，不把大图塞入 localStorage。离线可编辑，游客设计不混入登录账户。
- 成品封面确认保存后，再从成品提取三种主色供专辑页色场使用；编辑中不反复改变页面背景。用户需拥有上传图片的使用权，首版不增加公开分享。

### 验收与首版取舍

- 验收模板切换、撤销、刷新恢复、手机裁切、键盘移动/图层操作、中文长标题、图片失效、存储不足、跨账号隔离及导出一致性。
- 图层控件提供按钮替代拖拽，触控区域至少 44px。
- 首版不加入 AI 生图、画笔、复杂滤镜、任意字体上传或多人协作；先保证能做出有个人辨识度且可再次编辑的封面。

## 审核点

请审核入口位置、左右编选布局、手机三步流程及上述封面编辑范围。当前仅新增计划和生成草图，未新增网站功能。
